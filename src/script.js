// This file is currently unnecessary as this chatbox doesn't use JavaScript to function at all.
// It will be kept here as to not confuse people and in case KogaChat uses JavaScript at some point.
// Commented below is the default code used in Streamlabs' chatbox.

/*
// Please use event listeners to run functions.
document.addEventListener('onLoad', function(obj) {
	// obj will be empty for chat widget
	// this will fire only once when the widget loads
});

document.addEventListener('onEventReceived', function(obj) {
  	// obj will contain information about the event
	
});
*/
